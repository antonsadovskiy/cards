import React from "react";
import Skeleton from "@mui/material/Skeleton";

const CardSkeleton = () => {
  return <Skeleton variant="rectangular" width={422} height={162} />;
};

export default CardSkeleton;
